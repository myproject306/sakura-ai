# ══════════════════════════════════════════
# Sakura AI — Environment Variables
# Copy this file to .env and fill in values
# NEVER commit .env to version control
# ══════════════════════════════════════════

# ── Server ──
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5500

# ── Database ──
# SQLite (development):
DATABASE_URL="file:./dev.db"
# PostgreSQL (production):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/sakura_ai?schema=public"

# ── JWT ──
JWT_SECRET=change-this-to-a-long-random-secret-min-32-chars
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=change-this-refresh-secret-also-min-32-chars
JWT_REFRESH_EXPIRES_IN=30d

# ── Google OAuth ──
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# ── Stripe ──
# Use TEST keys for development, LIVE keys for production
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Create these products/prices in Stripe Dashboard first:
STRIPE_STARTER_PRICE_ID=price_starter_monthly
STRIPE_PRO_PRICE_ID=price_pro_monthly
STRIPE_TEAM_PRICE_ID=price_team_monthly
# Free trial days (set to 0 to disable)
STRIPE_TRIAL_DAYS=7

# ── OpenAI ──
OPENAI_API_KEY=sk-...
OPENAI_MODEL_TEXT=gpt-4-turbo-preview
OPENAI_MODEL_CODE=gpt-4-turbo-preview

# ── Stability AI (Image Generation) ──
STABILITY_API_KEY=sk-...
STABILITY_API_HOST=https://api.stability.ai

# ── ElevenLabs (Text-to-Speech) ──
ELEVENLABS_API_KEY=...
ELEVENLABS_VOICE_ID_EN=21m00Tcm4TlvDq8ikWAM
ELEVENLABS_VOICE_ID_AR=your-arabic-voice-id

# ── Redis (Bull Queue — optional in dev, required in prod) ──
# Leave empty to use in-memory fallback in development
REDIS_URL=redis://localhost:6379

# ── Admin ──
ADMIN_EMAIL=admin@sakura-ai.com
ADMIN_SECRET=change-this-admin-secret

# ── Rate Limiting ──
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
TOOL_RATE_LIMIT_MAX=20

# ── Usage Limits per Plan ──
STARTER_MONTHLY_TOKENS=500000
PRO_MONTHLY_TOKENS=2000000
TEAM_MONTHLY_TOKENS=10000000
STARTER_MONTHLY_CREDITS=50
PRO_MONTHLY_CREDITS=200
TEAM_MONTHLY_CREDITS=1000

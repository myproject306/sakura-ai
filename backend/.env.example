# ══════════════════════════════════════════
# Sakura AI — Environment Variables
# Copy this file to .env and fill in values
# ══════════════════════════════════════════

# ── Server ────────────────────────────────
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:5500

# ── Database ──────────────────────────────
DATABASE_URL="file:./dev.db"

# ── JWT Auth ──────────────────────────────
JWT_SECRET=your_super_secret_jwt_key_here_change_this
JWT_EXPIRES_IN=7d

# ── Google OAuth ──────────────────────────
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ══════════════════════════════════════════
# HIDDEN AI ENGINE CONFIGURATION
# Users will never see these provider names.
# All results appear as "Sakura AI" output.
# ══════════════════════════════════════════

# ── AI Provider Mode ──────────────────────
# Options:
#   both    → Round-robin between Copilot & Gemini (RECOMMENDED)
#   copilot → Always use CopilotSearch (Azure OpenAI)
#   gemini  → Always use Google Gemini
#   auto    → Use whichever key is set (Copilot → Gemini → OpenAI)
#   openai  → Use OpenAI directly (legacy fallback)
AI_PROVIDER=both

# ── CopilotSearch (Azure OpenAI) ──────────
# Get from: https://portal.azure.com → Azure OpenAI
COPILOT_API_KEY=your_azure_openai_api_key
COPILOT_ENDPOINT=https://YOUR-RESOURCE-NAME.openai.azure.com
COPILOT_DEPLOYMENT=gpt-4o
COPILOT_API_VERSION=2024-02-01

# ── Bing Search (Real-time enrichment) ────
# Get from: https://portal.azure.com → Bing Search v7
# Used silently to enrich AI results with current web data
BING_SEARCH_API_KEY=your_bing_search_api_key

# ── Google Gemini ─────────────────────────
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-1.5-pro

# ── OpenAI (Fallback + Audio/STT) ─────────
# Used as last-resort fallback & for Whisper STT
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL_TEXT=gpt-4-turbo-preview
OPENAI_MODEL_CODE=gpt-4-turbo-preview

# ══════════════════════════════════════════
# MEDIA PROVIDERS (unchanged)
# ══════════════════════════════════════════

# ── Stability AI (Image generation) ───────
STABILITY_API_KEY=your_stability_api_key
STABILITY_API_HOST=https://api.stability.ai

# ── ElevenLabs (Text-to-Speech) ───────────
ELEVENLABS_API_KEY=your_elevenlabs_api_key
ELEVENLABS_VOICE_ID_EN=21m00Tcm4TlvDq8ikWAM
ELEVENLABS_VOICE_ID_AR=your_arabic_voice_id

# ── Stripe (Payments) ─────────────────────
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PRICE_PRO_MONTHLY=price_your_pro_monthly_id
STRIPE_PRICE_PRO_YEARLY=price_your_pro_yearly_id
STRIPE_PRICE_TEAM_MONTHLY=price_your_team_monthly_id
STRIPE_PRICE_TEAM_YEARLY=price_your_team_yearly_id

# ── Redis (Optional - for job queue) ──────
# REDIS_URL=redis://localhost:6379
